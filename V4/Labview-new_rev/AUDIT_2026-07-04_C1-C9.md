# Independent Audit — C1–C9 claims vs reality (2026-07-04)

Every claim re-verified by independent measurement (grep markers +
offscreen behavioral runs against case IDD5SL12WE + DataDOE80f.csv), per
DEBUGGING_METHODOLOGY. Verdict: **C1–C9 are genuinely done** — the best
implementation round so far — with TWO residual gaps (G1, G2 below) and
C10 still open as expected.

## Verdict table

| Item | Claim | Independent result |
|---|---|---|
| C1 version stamp + regen | ✅ | ✅ CONFIRMED earlier (5 diagrams regenerated, 143/143 mappings preserved) |
| C2 launch loop + title stamp | ✅ | ✅ CONFIRMED (title carries build + folder) |
| C3 mode removal | ✅ | ✅ CONFIRMED — `grep mode_combo` = 0 hits; toolbar = Edit layout / Analysis / Zoom to Fit / Mapping report |
| C4 product-sim naming | ✅ | ✅ CONFIRMED — buggy `shelf_column_name(0, 1, …)` call gone; 416 saved product labels, 0 duplicates |
| C5 instrument panel + theme | ✅ | ✅ CONFIRMED — `InstrumentPanelItem` in scene at runtime; `class SensorBoxItem` deleted; `diagram_theme.py` complete AND adopted (`import diagram_theme as theme`, 0 raw `QFont(` in components); `zoom_to_fit` fits width by default, Ctrl = full |
| C6 alias cleanup + conflicts UI | ✅ | ⚠️ MOSTLY — behavioral outcome correct: all four formerly-poisoned columns now map to their CORRECT canonicals (`T_air.ret.s11`, `ret.s10`, `fan_off.ctr.LE/RE`); `AVG Product temp`→`T_prod.avg`, `BTU`→`qc`, `Total Flow`→`m_dot`; Conflicts section exists in the report (sensor_panel.py:693). BUT see **G1**. |
| C7 integrity check | ✅ | ✅ CONFIRMED — `mapping_integrity_check` runs: 0 violations across 150 roles / 269 expected rows / 150 CSV columns; gaps: 0 unmapped CSV, 0 conflicts |
| C8 CSV visibility + ghost reveal | ✅ | ✅ CONFIRMED at marker level (ghost rendering present; 0 unmapped CSV columns; 118 no-data slots hidden). Final look needs owner eyes (G2). |
| C9 expansion follow-through | ✅ | ✅ CONFIRMED — parts schema has expansion kind (`txv/cap_tube/eev` + kind-specific fields); generator matrix measured: TXV→3 bulb dots; cap_tube→CapTube comps, 0 bulbs; EEV→EEV comps + 3 suction dots |
| C10 lexicon | not claimed | ☐ OPEN — next item (baseline: 343 labels, 115 unknown, worst case 47%) |

## Gaps found

### G1 — Stale poisoned alias survives in the alias DB (finish C6 properly)
The alias store still contains `Return Air 2 in RE → T_air.disc.s11` (a
return-air column pointing at a discharge slot). In the CURRENT flow it is
harmless — saved-case mappings and pattern resolution win, and the column
demonstrably lands on `T_air.ret.s11` — but on a FRESH case (no saved
mappings) resolution order could let this entry mis-map the column again.
Also note: the integrity check reported 0 violations while this
contradiction sat in the DB — i.e., **Invariant D (aliases may not
contradict pattern-derived canonicals) is not actually enforced by the
check.** Fix: purge the stale entry; add an Invariant-D audit pass to
`mapping_integrity_check` that scans the alias DB for entries whose target
family contradicts the pattern parser's result and reports them.

### G2 — Visual acceptance requires the owner's eyes
Offscreen audits can verify structure, not aesthetics. Owner should
double-click-launch and confirm, per OFF_DIAGRAM_INSTRUMENTS_PLAN
acceptance: (a) the Instrument Panel is visually indistinguishable in
style from the sections above it; (b) no grey placeholder holes anywhere;
(c) panel/diagram group names match; (d) width-fit lands at the top of the
page on case open. Any miss goes back as a normal bug with a screenshot.

## Next actions (in order)

1. G1 purge + Invariant-D enforcement (small; finishes C6 for real).
2. Owner visual pass (G2) — 10 minutes with the app.
3. C10 — the lexicon (grammar L0–L6): the single highest-value remaining
   item; 6 of 7 datasets currently below 100% coverage.
4. Then the diagnosis era begins per DIAGNOSIS_ROADMAP.md (Phase 1
   verdict-on-diagram + scorecard).
